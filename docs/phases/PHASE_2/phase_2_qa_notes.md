- ~~Need a distinction between "major" roles on "Bad" teams, and minor roles. The game must have at least 1 card in a "major" role for any given bad team. For example, if the only bad team is "Werewolf", there must be at least one card that is a kind of Werewolf. You cannot start a game with only a "Minion" on the werewolf team. We need a way in our data model to flag "primary" team roles and enforce a minimum 1 of a primary team role.~~ **Addressed — Stage 1 (is_primary_team_role column) + Stage 2 (backend validation)**
- ~~Does our code support the ability to create different bad teams? It should.~~ **Addressed — Verified by design: Team enum has VILLAGE, WEREWOLF, VAMPIRE, ALIEN, NEUTRAL; validation loop handles any non-Village/non-Neutral team (Stages 1–2)**
- ~~I would like an error message to display next to the "Start Game" button anytime the button is not valid but someone tries to press it anyway. They should get an error about why they can't press it. (not enough roles selected, too many roles selected, etc)~~ **Addressed — Stage 4 (Start Game button error messages)**
- ~~Players, Center Cards, and Discussion timer validation and clamp should occur only when someone clicks away from the field. At present it's impossible to set the Discussion Timer to anything besides the default, because if you erase the number 300 and start to write 100 for example, it immediately errors and goes to 600 because "1" is not valid. Validation needs to defer until a person leaves the field.~~ **Addressed — Stage 3 (deferred input validation, clamp on blur)**
- ~~Recommend dependencies are mentioned in QA, but no messages are observed. Should be a section above the "Start Game" button that reports any recommendations/warnings about the current selected card set.~~ **Addressed — Stage 5 (dependency recommendations panel)**
