# Phase 04A QA Coverage Map

**Date:** 2026-09-13  
**Scope:** Consolidated acceptance-criterion coverage for the three Phase 04A pure TypeScript engine features.

`None needed` means the existing focused unit or integration tests already prove the criterion. `Automated` means this QA package adds a deterministic command whose exit status or parsed output decides the result. No criterion needs hybrid or manual judgment.

| Feature | AC | Existing Test Coverage | QA Kind | Reason |
|---------|----|------------------------|---------|--------|
| 01-engine-types-templates | AC1 | `yourwolf-frontend/src/test/engine/templates.test.ts` constructs the readonly engine role and step shapes with the required fields. | None needed | The focused input-shape assertions and successful TypeScript build prove the declared contract. |
| 01-engine-types-templates | AC2 | `yourwolf-frontend/src/test/engine/templates.test.ts` asserts exact keys from actual `buildRoleScript()`, `buildNightScript()`, and `buildPreview()` results. | None needed | The production builders supply every asserted narrator output, and a field-removal mutation fails the test. |
| 01-engine-types-templates | AC3 | `templates.test.ts` transcribes the literal instruction matrix, and `yourwolf-backend/tests/test_narration_templates.py` is the reference oracle. | None needed | Existing exact-string tests already prove all supported template branches. |
| 01-engine-types-templates | AC4 | `templates.test.ts` covers self, team, role, underscore, empty, null, and unknown wake targets. | None needed | Existing exact-string wake cases already prove the required rendering and fallthrough behavior. |
| 01-engine-types-templates | AC5 | `templates.test.ts` covers all duration cases, unknown instructions, and inherited property names. | None needed | Existing duration and unknown-type assertions already prove the lookup behavior. |
| 01-engine-types-templates | AC6 | `templates.test.ts` pins both the `Werewolfs` thumbs-up copy and the `Werewolves` wake copy. | None needed | Existing literal assertions already prove the frozen spelling distinction. |
| 01-engine-types-templates | AC7 | `yourwolf-frontend/eslint.config.js` defines the engine boundary; the engine modules contain only local or permitted domain type imports. | Automated | Check `A10` rejects forbidden imports and external or ambient runtime dependencies with a deterministic exit status. |
| 01-engine-types-templates | AC8 | The implementation contains no direct domain step-editing helper call. | Automated | Check `A10` fails on any `setStepModifier`, `moveStepUp`, or `moveStepDown` reference. |
| 01-engine-types-templates | AC9 | `templates.test.ts` is the focused parity suite; package scripts define lint, build, and coverage gates. | Automated | Checks `A1`, `A3`, `A5`, `A6`, `A7`, and `A8` decide the test, lint, build, and coverage outcomes. |
| 02-narration-scripts-preview | AC1 | `yourwolf-frontend/src/test/engine/narration.test.ts` covers opening/closing actions, unique role blocks, and duration sums. | None needed | Existing focused assembly assertions already prove the criterion. |
| 02-narration-scripts-preview | AC2 | `narration.test.ts` covers positive wake filtering, null/zero exclusion, name tie-breaking, and empty-sequence defaulting. | None needed | Existing focused ordering assertions already prove the criterion. |
| 02-narration-scripts-preview | AC3 | `narration.test.ts` covers unknown ids, repeated ids, named-first ordering, unnamed fallback ordering, and empty sequences. | None needed | Existing focused custom-order assertions already prove the criterion. |
| 02-narration-scripts-preview | AC4 | `narration.test.ts` covers dense step assembly, unknown inherited names, OR flags, required flags, and no-step roles. | None needed | Existing focused role-script assertions already prove the criterion. |
| 02-narration-scripts-preview | AC5 | `narration.test.ts` covers wake guards, copied-role headers, both second-wake types, sequential order, and one-header behavior. | None needed | Existing focused preview assertions already prove the criterion. |
| 02-narration-scripts-preview | AC6 | `narration.test.ts` compares default and complete custom output with `night-script.fixture.json`. | None needed | The committed fixture comparison already proves seed-role night-script parity. |
| 02-narration-scripts-preview | AC7 | `narration.test.ts` compares every fixture role preview and explicitly checks the empty Villager and Tanner previews. | None needed | The committed fixture comparison already proves seed-role preview parity. |
| 02-narration-scripts-preview | AC8 | `narration.test.ts` checks fixture input keys; the implementation and review records document one-time Python generation. | Automated | Check `A9` independently converts seed data, regenerates Python outputs, and checks the static fixture assets. Check `A10` confirms no generator or extra runtime asset remains. |
| 02-narration-scripts-preview | AC9 | `narration.test.ts` covers the listed edge cases; package scripts define full test, lint, build, and coverage gates. | Automated | Checks `A1`, `A3`, `A5`, `A6`, `A7`, and `A8` decide the test, lint, build, and coverage outcomes. |
| 03-game-session-state-machine | AC1 | `yourwolf-frontend/src/test/engine/gameSession.test.ts` covers the create shape, caller values, nested input data, injected id, and deep immutability. | None needed | Full deep snapshots cover success and rejection; a nested-parameter mutation fails the test. |
| 03-game-session-state-machine | AC2 | `yourwolf-frontend/src/test/engine/gameSetupValidation.test.ts` covers ordered rejection and `gameSession.test.ts` routes rejection through create. | None needed | Existing precedence and atomicity assertions already prove the criterion. |
| 03-game-session-state-machine | AC3 | `gameSetupValidation.test.ts` and `gameSession.test.ts` cover required dependency errors, recommended warnings, and propagation. | None needed | Existing exact-message and warning assertions already prove the criterion. |
| 03-game-session-state-machine | AC4 | `gameSetupValidation.test.ts` covers duplicate, extra, non-waking, missing, empty, null, omitted, and valid sequences. | None needed | Existing focused sequence assertions already prove the criterion. |
| 03-game-session-state-machine | AC5 | `gameSession.test.ts` requires an injected generator spy to run once and requires its return value to become `session.id`. | Automated | The spy fails when generation is bypassed; check `A10` also rejects ambient crypto, time, random, persistence, and network references. |
| 03-game-session-state-machine | AC6 | `gameSession.test.ts` covers setup-only start, night transition, zero wake index, rejection, and deep input immutability. | None needed | A populated session snapshot covers nested collections, and a nested-array mutation fails the test. |
| 03-game-session-state-machine | AC7 | `gameSession.test.ts` covers setup and terminal rejection, every valid phase step, the full path, and deep input immutability. | None needed | Successful and rejected transitions preserve deep snapshots of populated sessions. |
| 03-game-session-state-machine | AC8 | `gameSetupValidation.test.ts` transcribes the setup oracle, including both duplicate precedence seams and the explicit null case. | None needed | Existing focused tests and the read-only backend oracle suite already prove the transcribed cases. |
| 03-game-session-state-machine | AC9 | The engine modules contain no session integration or excluded runtime capability. | Automated | Check `A10` rejects excluded capabilities, runtime callers, out-of-scope application changes, and extra fixture assets. |
| 03-game-session-state-machine | AC10 | Focused engine tests and package scripts define the full test, lint, build, and coverage gates. | Automated | Checks `A1`, `A2`, `A3`, `A4`, `A5`, `A6`, `A7`, and `A8` decide the required verification outcomes. |
